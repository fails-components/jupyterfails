# Jupyter lite deployment
This directory contains the build files used for * fails-components * together with the * @fails-components/jupyter-launcher * packages integrate jupyter with an iframe into fails.

