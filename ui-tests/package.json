{
  "name": "@fails-components/jupyter-applet-view-ui-tests",
  "version": "0.0.1",
  "description": "JupyterLab @fails-components/jupyter-applet-view Integration Tests",
  "private": true,
  "scripts": {
    "start": "jupyter lab --config jupyter_server_test_config.py",
    "test": "jlpm playwright test",
    "test:update": "jlpm playwright test --update-snapshots"
  },
  "devDependencies": {
    "@jupyterlab/galata": "^5.0.5",
    "@playwright/test": "^1.37.0"
  }
}
